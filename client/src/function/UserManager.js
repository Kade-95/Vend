/* eslint-disable */
export function UserManager(kerdx, api) {
    let self = {};

    self.register = () => {
        let form = document.body.find('#registration-form');
        console.log(form);
    }

    return self;
}